

"use strict";

/**
 * @constant config it contains a platformSupport array which is a list of all the platforms currently supported by the library
 */
const config = {
    platformSupport: [
        "TEXT",
        "ACTIONS_ON_GOOGLE",
        "FACEBOOK_MESSENGER",
        "TELEPHONY"
    ]
}

module.exports = config;
